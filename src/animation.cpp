#include "animation.h"

Animation::Animation()
{
    translation.setX(0);
    translation.setY(0);
    translation.setZ(0);

    rotation.setX(0);
    rotation.setY(0);
    rotation.setZ(0);
}
